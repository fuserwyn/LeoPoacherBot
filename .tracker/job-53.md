# Задача трекера #49

Задача #49.

Нужно сделать для  ачивкой отдельную выпадашку и для больничного выпадашку здоровье и там кнопку взять больничный

## выполнение

Сделал сворачиваемые разделы в профиле по паттерну «Уведомления» / «Тема».

**Ачивки** — отдельная выпадашка с шевроном, по умолчанию свёрнута. В заголовке видно счётчик (`3/14`), внутри — полоски ачивок за стрики и тренировки.

**Здоровье** — отдельная выпадашка с кнопкой «Взять больничный» внутри (форма заявки и «Выйти с больничного» тоже там). В свёрнутом виде показывается статус: «на больничном» или «заявка». При сворачивании форма сбрасывается.

Изменения в `ProfileScreen.tsx`, `ProfileScreen.css`, `index.css` (стили карточек для розовой темы). Локальный коммит не создан — в окружении не настроен git user.

## ревью

Ревью: на ветке tracker/49-514 config.go целый. Можно на тест.
