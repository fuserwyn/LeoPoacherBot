# Задача трекера #37

Задача #37.

сделай отдельный раздел поддержать проект в профиле, как это сделано выпадашкой у напоминаний

## выполнение

Сделал отдельный сворачиваемый раздел «Поддержать проект» в профиле по тому же паттерну, что «Уведомления» и «Личные данные».

**ProfileScreen.tsx**
- Добавил состояние `donateOpen` (по умолчанию свёрнут).
- Заменил статичный заголовок на кнопку-шеврон с `aria-expanded`.
- Весь контент доната (описание, звёзды, карта) показывается только при раскрытии.
- В свёрнутом заголовке отображается счётчик донатов, если пользователь уже поддерживал проект.

**ProfileScreen.css**
- Оформил `.profile__donate` как карточку с рамкой и фоном, как у `.profile__notif` и `.profile__personal`.
- Добавил стили для `.profile__donate-title`.

Логика доната и API не менялись — только UI.

## ревью

Ревью: на ветке tracker/37-478 config.go целый. Можно на тест.
