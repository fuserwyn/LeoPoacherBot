# Задача трекера #40

Задача #40.

пиши еще в уведомлении название задачи, а не только ее номер

## выполнение

Сделано для задачи #40 «пиши еще в уведомлении название задачи, а не только ее номер».

**ms_leo** — добавлены хелперы `trackerTaskTitle`, `trackerNotifyLabel`, `trackerNotifyHeading`. Название берётся из первой строки prompt (без префиксов «Задача #N» и «[Спринт N]»). Обновлены:
- `trackerFullyDoneNote` — «Задача #40: название выполнена»
- `trackerDueStartedNote` — «Задача #40: название началась»
- `trackerPipelineNotify` — сообщения о публикации

**ms_tracker** — аналогичная логика в `notify.go`: из prompt агента извлекается название (в т.ч. из блока «Формулировка:» для review/test). Обновлены все уведомления в `notifyText` и сообщение об ошибке старта агента в `worker.go`.

**Тесты** — добавлены/обновлены в `tracker_run_test.go`, `tracker_notify_test.go`, `notify_test.go`.

Локальный коммит не создан: в окружении не настроен git user (конфиг не трогаю). Трекер может закоммитить при пуше.
