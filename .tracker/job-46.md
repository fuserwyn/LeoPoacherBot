# Задача трекера #42

Задача #42.

сделай в админке дашборды

## выполнение

Сделано для задачи #42.

**Новый экран «Дашборды»** (`AdminDashboardScreen.tsx` + стили):
- переключатель периода (7 / 30 / 90 дней, всё время);
- карточки посещений бота;
- сетка KPI с подсветкой активации (зелёный ≥35%, красный <20%);
- воронки «бот → оплата» и «активация» в виде полос;
- retention-события компактными барами.

Данные берутся из существующих API `fetchAdminAnalytics` и `fetchAdminVisits` — бэкенд не менялся.

**AdminScreen.tsx**:
- во вкладке «Система» добавлена плитка «Дашборды»;
- «Аналитика» оставлена для подробных таблиц;
- у «Посещения бота» сменена иконка, чтобы не дублировать дашборды.

Путь: **Профиль → Админка → Система → Дашборды**.

Локальный коммит не создан — в окружении не настроен git user (конфиг не трогал). Изменения в рабочей копии, трекер может закоммитить при пуше.
