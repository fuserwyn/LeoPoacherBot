# Задача трекера #65

Задача #65.

В профиле донаты заехали за рамку 
Исправь

## выполнение

Исправил переполнение блока донатов в профиле.

**ProfileScreen.css**
- Секция `.profile__donate` — `min-width: 0` и `overflow: hidden`, чтобы содержимое не выходило за скруглённую карточку.
- Ряд номиналов `.profile__donate-tiers` — `flex-wrap: wrap`, кнопки по ~3 в строке (`flex: 1 1 calc(33.333% - 4px)`, `min-width: 0`).
- Подписи и методы оплаты — `overflow-wrap` / `min-width: 0`, чтобы длинный текст не раздвигал блок.

**index.css (розовая тема)**
- «Поддержать проект» снова оформлен как карточка (как «Уведомления» и «Тема»), убран override с `padding: 0` и без рамки.

Локальный коммит не создан: в окружении не настроены `git user.name` / `git user.email`. Изменения в рабочей копии на ветке `tracker/65-577`.

## ревью

Ревью: на ветке tracker/65-577 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/65-577. Тест пройден.
