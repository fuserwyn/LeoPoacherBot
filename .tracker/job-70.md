# Задача трекера #66

Задача #66.

Сейчас, когда берешь больничный - сгорает стрик. Необходимо проверить и исправить, если такая проблема есть. Ожидаемое поведение: когда я беру больничный мой стрик не сгорает, а значение замораживается, пока я не выйду из больничного.

Прошлый результат / замечания ревью:
⚠️ Задача #66: агент не стартовал.
нет правок в репозитории: агент сдал только заметку

## выполнение

**Задача #66 выполнена.**

**Проблема:** на бэкенде стрик при больничном уже замораживался (`streak_sick_leave.go`, `GetMiniappProfileStatsForAPI`), но мини-апп показывал 0. `effectiveStreakDays` в `streakLabel.ts` пересчитывал дни только по `last_training_date` и не учитывал скорректированный `days_since_last_training` с API.

**Правки:**
1. **`miniapp/src/lib/streakLabel.ts`** — добавлена `resolveStreakIdleDays`: если «сырая» дата даёт больше дней, чем API (+1), стрик считается замороженным (больничный). В этом режиме стрик не сгорает и не показывается таймер «сгорит через…».
2. **`miniapp/src/lib/streakLabel.test.ts`** — тест на сценарий больничного.
3. **`ms_leo/internal/bot/training.go`** — `calculateTrainingDayOutcome` учитывает дни больничного при превью стрика в боте (как при фактическом `#training_done`).

Тесты фронта (`streakLabel.test.ts`) — 25/25 пройдены.

Локальный коммит не создан: в окружении не настроен git user (config не трогал). Изменения в рабочей директории, трекер может закоммитить и запушить в `tracker/66-581`.
