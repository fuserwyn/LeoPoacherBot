# Задача трекера #51

Задача #51.

Перенеси кнопку спасти стрик над выпадашкой ачивки

## выполнение

Перенёс блок «Спасти стрик» в `ProfileScreen.tsx`: кнопка и подсказка теперь идут сразу после индикатора «стрик X» и **перед** выпадашкой «Ачивки», а не после неё.

Логика и стили не менялись — только порядок элементов в разметке. Локальный коммит не создан: в окружении не настроен git user (конфиг не трогал). Изменения в рабочей копии, трекер может закоммитить при пуше.
