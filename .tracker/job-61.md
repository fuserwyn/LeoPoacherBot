# Задача трекера #57

Задача #57.

Передвигуть заголовок в центр шапки раздела поддержка и правила стаи

## выполнение

Сделано.

**Поддержка** — в `ChatScreen.css` центрирование шапки теперь для всех чатов, включая `support-chat`: аватар и блок «Поддержка» / подпись выровнены по центру (`justify-content: center`, `align-items: center`, `text-align: center`).

**Правила Стаи** — в `RulesScreen.css` у заголовка «Правила Стаи» добавлен `text-align: center`.

Локальный коммит не создан: в окружении не настроен git user (по правилам git config не трогал). Изменения в рабочей директории готовы к пушу трекером.
