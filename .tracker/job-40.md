# Задача трекера #38

Задача #38.

сделай кнопку удалить задачу в виде корзинки с модалкрй подтверждения прямо с доски трекера

## выполнение

Сделано.

**Кнопка-корзина на карточке.** На карточках с `can_delete` в шапке справа появилась кнопка 🗑. Клик по ней не открывает карточку (`stopPropagation`).

**Модалка подтверждения.** Перед удалением показывается диалог с номером задачи, текстом и предупреждением о необратимости. Кнопки «Отмена» и «Удалить».

**Удаление.** После подтверждения вызывается существующий API `trackerDelete`, доска обновляется; если была открыта модалка этой задачи — она закрывается.

Изменения в `TrackerScreen.tsx` и `TrackerScreen.css`. Локальный коммит не создан — в окружении не настроен git user (по правилам конфиг не трогаю); трекер может закоммитить при пуше.

## ревью

Ревью: на ветке tracker/38-481 config.go целый. Можно на тест.
