# Задача трекера #68

Задача #68.

При наведении на поле ввода клавиатура перекрывает поле ввода

## выполнение

Сделано для задачи #68.

**`useAppKeyboardInset.ts`**
- Исправлен расчёт `--app-keyboard-bottom`: раньше при фокусе брался только Telegram `viewportStableHeight − viewportHeight`, который ещё не успевал обновиться → inset оставался 0 и поле уходило под клавиатуру. Теперь до обновления TG используется `visualViewport`, после — более точное значение из TG.
- Для компоуз-поля в таббаре (`bottom-nav__compose-input`) класс `app-nav-compose-open` выставляется глобально — таббар сразу поднимается над клавиатурой.
- `scrollIntoView` не вызывается для fixed-компоузеров (чат, таббар, админка) — они сами двигаются через CSS; для остальных полей используется `nearest`/`end` вместо `center`.

**`TrackerScreen.css`, `AdminScreen.css`**
- У полей ввода добавлен `scroll-margin-bottom` с учётом высоты клавиатуры — при фокусе в скроллируемых формах (трекер, админка) поле докручивается над клавиатурой.

Сборка `npm run build` в miniapp проходит. Локальный коммит не создан — в окружении не настроен git user.

## ревью

Ревью: на ветке tracker/68-587 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/68-587. Тест пройден.
